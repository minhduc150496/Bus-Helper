@using BusHelperDAL
@{
    ViewBag.Title = "Index";
    History obj = ViewBag.obj;
    List<BusStop> busStops = ViewBag.BusStops;
    List<UserInfo> userInfos = ViewBag.UserInfos;
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        History
    </h1>
</section>

<!-- Main content -->
<section class="content">
    <!-- general form elements -->
    <div class="box box-primary">
        <!-- form start -->
        <form role="form" action="@Url.Action("Update", "History")" method="post">
            <input type="hidden" name="id" value="@obj.id" />
            <div class="box-body">
                <div class="form-group">
                    <label for="bus_stop_id">Bus Info</label>
                    <select name="bus_stop_id" id="bus_stop_id" class="form-control">
                        @foreach (var item in busStops)
                        {
                            <option @((item.id == obj.bus_stop_id) ? "selected" : "") value="@item.id">@item.name</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <label for="user_id">User Info</label>
                    <select name="user_id" id="user_id" class="form-control">
                        @foreach (var item in userInfos)
                        {
                            <option @((item.id == obj.user_id) ? "selected" : "") value="@item.id">@item.username</option>
                        }
                    </select>
                </div>
            </div>
            <!-- /.box-body -->

            <div class="box-footer">
                <button type="submit" class="btn btn-primary">Update</button>
            </div>
        </form>
    </div>
    <!-- /.box -->
</section>